function showPage(name){document.querySelectorAll(".page").forEach(p=>p.classList.toggle("active",p.id==="page-"+name));window.scrollTo(0,0);if(name!=="game"&&game.running){game.running=false}if(name==="game"){game.start()}}document.addEventListener("click",e=>{const b=e.target.closest("[data-page]");if(b){e.preventDefault();showPage(b.dataset.page)}});el("playHome").onclick=()=>showPage("game");el("settingsTop").onclick=()=>showPage("settings");document.querySelectorAll("[data-board]").forEach(b=>b.onclick=()=>{document.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));b.classList.add("active");renderLeaderboard(b.dataset.board)});function boot(){applyTheme();renderAll();showPage("home");window.addEventListener("beforeunload",saveState)}boot();